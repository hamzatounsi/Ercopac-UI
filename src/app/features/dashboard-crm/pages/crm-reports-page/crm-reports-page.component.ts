import { Component,OnInit } from '@angular/core';import { CrmBreakdown,CrmReports } from '../../models/crm-detail.model';import { CrmService } from '../../services/crm.service';
@Component({selector:'app-crm-reports-page',templateUrl:'./crm-reports-page.component.html',styleUrls:['./crm-reports-page.component.scss']})
export class CrmReportsPageComponent implements OnInit{orgId=this.crm.getOrgIdFromToken();reports?:CrmReports;loading=true;error='';active:'country'|'stage'|'category'|'value'='country';constructor(private crm:CrmService){}ngOnInit():void{this.crm.getReports(this.orgId).subscribe({next:v=>{this.reports=v;this.loading=false;},error:e=>{this.error=e?.error?.message||'Unable to load reports.';this.loading=false;}});}money(v:number):string{return new Intl.NumberFormat('en',{style:'currency',currency:'EUR',maximumFractionDigits:0}).format(v||0);}max(values:CrmBreakdown[]):number{return Math.max(1,...values.map(v=>v.count));}}
